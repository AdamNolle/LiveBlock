[package]
name = "liveblock-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Core traits and coordinator for LiveBlock cross-platform engine"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
parking_lot = { workspace = true }
liveblock-regions = { path = "../liveblock-regions" }
liveblock-labels = { path = "../liveblock-labels" }
liveblock-detection = { path = "../liveblock-detection" }
liveblock-tracker = { path = "../liveblock-tracker" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
