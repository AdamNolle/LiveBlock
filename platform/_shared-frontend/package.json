{
  "name": "liveblocker-shared-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared Tauri webview frontend for the Windows + Linux LiveBlocker ports. Light-only neumorphism on a lavender base, ported verbatim from the Claude Design handoff bundle.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/cli": "2.11.1",
    "typescript": "^5.5.0",
    "vite": "6.4.3"
  }
}
