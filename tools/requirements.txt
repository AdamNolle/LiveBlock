ultralytics>=8.3.0
coremltools>=8.0
torch>=2.2
torchvision>=0.17
numpy>=1.26,<2.0
pillow>=10.0
pyyaml>=6.0
cryptography>=45.0
scikit-learn>=1.5,<=1.5.1  # coremltools 9 conversion API upper bound
huggingface_hub>=0.24
# Open-vocabulary build + ONNX export (Route A)
# CLIP text encoder used by ultralytics YOLOWorld.set_classes() to bake text
# embeddings into the head. ultralytics expects its own fork at this exact URL.
clip @ git+https://github.com/ultralytics/CLIP.git
onnx>=1.16
onnxruntime>=1.18
onnxslim>=0.1.34
ftfy>=6.1
regex>=2024.5
# Test runner for tools/ + tools/eval/
pytest>=8.0
